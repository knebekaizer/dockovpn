docker build -t dockvpn .

docker run -d -it --rm -v $PWD/client_data:/opt/Dockovpn_data/clients --cap-add=NET_ADMIN -p 1194:1194/udp -e HOST_ADDR=$(curl -s https://api.ipify.org) --name dockvpn dockvpn

docker run -d -it --rm -v $PWD/etc/openvpn:/etc/openvpn  -v $PWD/client_data:/opt/Dockovpn_data/clients --cap-add=NET_ADMIN -p 1194:1194/udp -e HOST_ADDR=$(curl -s https://api.ipify.org) --name dockvpn dockvpn

docker exec dockvpn ./genclient.sh foo

	$ tree client_data/
	client_data/
	├── foo
	│   ├── ca.crt
	│   ├── client.ovpn
	│   ├── foo.crt
	│   ├── foo.key
	│   └── ta.key
	└── guOFti5yFvRGZVmOP0uTOPeTrTjXWlqS
	    ├── ca.crt
	    ├── client.ovpn
	    ├── guOFti5yFvRGZVmOP0uTOPeTrTjXWlqS.crt
	    ├── guOFti5yFvRGZVmOP0uTOPeTrTjXWlqS.key
	    └── ta.key

